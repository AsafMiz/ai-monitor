# ===================================================================
# .dockerignore — AI Monitor API (Python)
# Only apps/api/requirements.txt and apps/api/src/ enter the build.
# Everything else is excluded via whitelist approach.
# ===================================================================

# Exclude everything
*

# Whitelist only what the Dockerfile needs
!apps/api/requirements.txt
!apps/api/src/
!apps/api/src/**
